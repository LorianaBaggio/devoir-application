export interface UserRegister {
    username ? : string;
    first_name: string;
    last_name: string;
    email: string;
    password: string;
    phone: string;
    avatar ? : string;
    confirm_password: string;
}