export interface User {
    userId ?: string;
    username ? : string;
    first_name: string;
    last_name: string;
    email: string;
    password: string;
    avatar ? : string;
    createdAt: number;
    phone: string;
}

